


// Instantiate the module
registerFile instance_name (
    .rs(rs), 
    .rt(rt), 
    .rd(rd), 
    .writeSig(writeSig), 
    .Clk(Clk), 
    .writeData(writeData), 
    .sourceReg(sourceReg), 
    .secondaryReg(secondaryReg)
    );


